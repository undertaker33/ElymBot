package com.astrbot.android.core.runtime.context

/**
 * Pure marker for future runtime-context contributors.
 *
 * The legacy app-held runtime context DTOs still expose feature/app models and
 * must be DTO-ized before they can move into this module.
 */
interface RuntimeContextContributor
