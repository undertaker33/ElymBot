# D26051901-T01 插件指令管理员权限 Todo

更新时间：2026-05-20 00:00 +08:00

## 范围

实现配置开关 `pluginCommandsAdminOnlyEnabled`，并在 QQ 斜杠指令入口按管理员 UID 做权限拦截。该入口包括内置 bot command 和插件 command fallback。

## 允许修改

- `feature/config/**`
- `feature/qq/runtime/**`
- `feature/plugin/runtime/**`
- `core/db/**`
- `core/runtime-context/**`
- `app/src/main/java/com/elymbot/android/core/db/backup/**`
- `app/src/main/java/com/elymbot/android/di/runtime/context/**`
- `app/src/main/res/**`
- `feature/voiceasset/api/src/main/res/**`
- `app/src/test/**`
- `app/schemas/**`
- `core/db/schemas/**`
- 当前任务包和 `docs/current-state.md`

## 禁止修改

- 不执行 Git 写入。
- 不改插件协议和插件包格式。
- 不改 QQ 登录、NapCat bridge、wake word、白名单、限流主策略。
- 不更新 `docs/context/**`；如需长期上下文同步，交给 `uth-docs` scoped sync。

## 实施步骤

1. 增加 config 字段并贯通持久化、备份、runtime snapshot。
2. 增加配置页开关和中英文字符串。
3. 在 QQ bot command 和 plugin command dispatch 前接入管理员权限策略。
4. 增加 schema / mapper / permission policy 测试。
5. 执行 fresh verification 并记录结果。

## 验证命令

```powershell
.\gradlew.bat :app:testDebugUnitTest --tests "com.elymbot.android.feature.qq.runtime.QqPluginDispatchServiceTest" --tests "com.elymbot.android.data.db.ElymBotDatabaseSchemaContractTest" --tests "com.elymbot.android.data.db.config.ConfigMappersTest" --console=plain --no-daemon --stacktrace
.\gradlew.bat clean architectureCheck assembleDebug --console=plain --no-daemon --stacktrace
.\gradlew.bat :app:testDebugUnitTest --console=plain --no-daemon --stacktrace
```

## 完成判定

- 代码实现完成。
- 三条验证命令 fresh 通过。
- 全量日志 warning / deprecated / exception 扫描为 0。
- Feedback 写入当前任务包。
