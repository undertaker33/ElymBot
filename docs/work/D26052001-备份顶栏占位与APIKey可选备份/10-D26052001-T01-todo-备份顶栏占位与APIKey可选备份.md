# D26052001-T01 Todo：备份顶栏占位与 API-Key 可选备份

## 任务

实现 `00-D26052001-design.md` 中的全部需求。

## 步骤

1. 调整 `SettingsSubPageScaffold`，让备份页内容区给全局 secondary top bar 和 safe drawing 留出顶部占位。
2. 调整全局顶栏备份 route 标题映射，让备份三级页显示对应模块标题。
3. 为完整备份和模型备份新增确认弹窗，包含默认关闭的“是否备份 API-Key”开关。
4. 将 `includeProviderApiKeys` 从 UI 透传到 backup repository。
5. 修改 Provider JSON 写出逻辑：默认清空 `apiKey`，开启开关时保留原值。
6. 添加或更新直接相关测试，覆盖默认不导出 API-Key 和开启后导出 API-Key。
7. 运行针对性测试和必要构建验证。

## 非目标

- 不修改数据库 schema / migration。
- 不扩大 backup 覆盖范围到 Resource Center、Cron 或 Plugin。
- 不处理 D26051901 以外的旧改动。
- 不执行 Git 写入。

## 完成标准

- 用户列出的 3 个需求均落地。
- 相关测试通过。
- 最终验证命令通过，warning / deprecated / exception 扫描计数为 0。
