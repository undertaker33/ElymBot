# 状态规则

- `docs/current-state.md` 是当前状态索引，不是变更日志。
- stale facts、完成任务流水、历史 Run Log 不留在 current-state 中。
- onboarding handoff snapshot 是一次性交接证据。
- 完整接管完成前，不要把 `Needs uth-docs confirmation from code facts.` 改成已确认事实。
