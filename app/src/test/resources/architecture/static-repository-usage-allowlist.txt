# path | symbol | owner | target | reason | expires | issue
#
# Rules:
# 1. Entries must be exact production Kotlin file paths relative to app/src/main/java/com/astrbot/android.
# 2. Do not add directory-level allowlist entries.
# 3. Do not use wildcards.
# 4. owner, target, reason, expires, and issue must be non-empty.

ui/navigation/AstrBotAppScaffoldParts.kt | FeatureConfigRepository | app-shell:navigation | app shell config selection port migration | app navigation shell still resolves selected config through the legacy static config repository facade; remove when config selection is supplied by an injected navigation/ViewModel state port | phase-21-app-shell-port-closure | phase-20-final-review
