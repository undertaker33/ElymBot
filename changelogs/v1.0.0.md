# v1.0.0

## 新增

- 将 Android app 自有标识从 AstrBot 统一迁移为 ElymBot，覆盖 `applicationId`、Gradle `namespace`、app shell、Hilt wiring、feature 模块、运行时脚本、Room schema owner、测试源码和插件示例。
- 当前项目文档与治理上下文统一采用 ElymBot 命名基线。
- 使用 ElymBot 项目 hook 与包名口径重新生成模板插件包。

## 修复

- 清理当前 source set 和生成 schema owner 中剩余的项目自有旧 AstrBot package 路径。
- 保持包名迁移后的架构检查、build logic、app 单元测试和 debug APK 构建 gate 无告警通过。
