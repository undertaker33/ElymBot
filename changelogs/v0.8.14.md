# v0.8.14

## 新增

- 新增 `feature/provider/runtime` 模块和 `DefaultProviderRuntimePort`。
- 新增 `feature/voiceasset/api`、`feature/voiceasset/data`、`feature/voiceasset/presentation` 分层结构。
- 新增 Chat ViewModel runtime bindings adapter、Config transaction、Cron reconciliation 和 Download bootstrap 的 app-integration 绑定。
- 新增 runtime audio、runtime context、runtime persistence 和 voiceasset owner 的架构合同测试。

## 修复

- 修复 Provider runtime、语音资产、运行时资产、TTS 仓库和 Voice Clone 服务仍留在 app/core 混合 owner 的问题。
- 修复 ViewModel dependency、RuntimeAssetViewModel 和 VoiceAsset presentation compat 模块中的旧绑定残留。
- 修复备份仓库、LLM 音频端口和启动链在拆分后仍引用旧路径的问题。

